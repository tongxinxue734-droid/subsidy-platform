# 学生机云端部署指南

用阿里云/腾讯云学生机（学生认证后约 1 元/年），把平台部署到云端，7×24 在线访问，不依赖你的电脑。

## 一、购买学生机

1. **阿里云**：搜「阿里云 飞天加速计划 / 学生优惠」→ 完成学生认证 → 购买学生云服务器（约 1 元/年）
2. **腾讯云**：搜「云+校园」→ 学生认证 → 购买学生机
3. 配置选最低配即可（1 核 2G 足够跑这个系统），**系统镜像选 Ubuntu 22.04**（或 20.04）

## 二、服务器安装 Docker（一次性）

SSH 登录服务器（阿里云/腾讯云控制台都有网页版终端）：

```bash
curl -fsSL https://get.docker.com | sh
docker --version   # 出现版本号即成功
```

## 三、上传项目代码到服务器

在你**自己电脑**上，进入项目上一级目录，用 scp 上传：

```powershell
# Windows 本机执行（PowerShell）
scp -r 养老补贴 root@服务器公网IP:/root/
```

> 或把你的项目推到 GitHub/Gitee，服务器上 `git clone` 下来（更推荐，方便以后更新）。

## 四、一键部署

SSH 登录服务器后：

```bash
cd 养老补贴
docker compose up -d --build
```

- 首次构建约 3~5 分钟（拉镜像 + 装依赖）
- 首次启动后端会自动生成演示数据（约 1~3 分钟，100 万条记录）

## 五、访问

- **前端**：`http://服务器公网IP`（80 端口）
- **后端 API**：`http://服务器公网IP:8000`

## 六、配置密钥（建议）

在服务器上设置环境变量后重新部署（不改代码、不硬编码密钥）：

```bash
export JWT_SECRET=换成你的随机字符串
export DEEPSEEK_API_KEY=换成你的key
docker compose up -d --force-recreate backend
```

## 七、更新代码后重新部署

```bash
cd 养老补贴
git pull                # 或重新 scp 上传
docker compose up -d --build
```

## 常见问题

| 问题 | 解决 |
|---|---|
| 访问 IP 打不开 | 去云控制台「安全组」放行 **80** 和 **8000** 端口 |
| 首次访问慢/白屏 | 数据正在生成，等 1~3 分钟刷新 |
| 80 端口被占用 | 改 docker-compose.yml 里的 `80:80` 为 `8080:80`，访问加 :8080 |

## PWA（手机"添加到主屏幕"像 App）

部署好后，手机浏览器打开前端地址 → 浏览器菜单选「**添加到主屏幕**」→ 就会像 App 一样全屏、有图标、可离线打开。
